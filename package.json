{
  "name": "kailasa",
  "version": "2.0.0",
  "private": true,
  "description": "Kailasa - Shiva Manifestation App",
  "scripts": {
    "build": "echo 'Static app — no build step needed'",
    "cap:sync": "cap sync android",
    "cap:build": "cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "cordova-plugin-purchase": "^13.11.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0",
    "typescript": "~5.4.0"
  }
}
